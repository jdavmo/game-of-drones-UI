export const environment = {
    production: true,
    config: {
        api: "http://localhost:3000",
        resource: {
            point: "api",
            version: "v1",
            app: "gameofdrones"
        }
    }
};
