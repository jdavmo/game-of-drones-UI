export const environment = {
    production: false,
    config: {
        api: 'http://localhost:3000',
        resource: {
            service: 'games',
            version: 'v1' // TODO: use version endpoint
        }
    }
};
